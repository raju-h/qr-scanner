version: "3.8"
services:
  db:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: qrscanner
      POSTGRES_PASSWORD: localdev
      POSTGRES_DB: qrscanner
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
